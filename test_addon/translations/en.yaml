---
configuration:

  oauth_app_key:
    name: Oauth app key
    description: The "app" key you generate via the Dropbox UI.

  oauth_app_secret:
    name: Oauth app secret
    description: The "app" secret you generate via the Dropbox UI.

  output:
    name: Output
    description: >-
      The target directory in your Dropbox to which you want to upload.

  keep_last:
    name: Keep last
    description: The number of backups to keep locally.

  filetypes:
    name: Filetypes
    description: File extensions of files to upload.
